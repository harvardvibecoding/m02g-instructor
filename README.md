Continuing on from the prior unit, this project contails the resulting output of the instructor's AI when prompted for impact score calculation with multiple factors in `people_headcount_app.py`.

To run the Streamlit app:

```bash
streamlit run people_headcount_app.py
```

